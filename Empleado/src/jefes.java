public interface jefes {
    // remember that the methods are public and abstract, thus if nothing is written this means the method is public
    // and abstract already
    // is the same to write public abstract or just leave the type to return, in this case the method tomar_decisiones
    // return a String
    public abstract String tomar_decisiones(String decision);
}
